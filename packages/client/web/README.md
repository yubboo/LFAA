# @lfaa/client-web

Web 产品的 Client Composition。拥有 React 根、Identity Gate → Smart Home → Workbench 页面级装配、Smart Home initial draft handoff 与浏览器侧 Host Adapter 组装；不拥有 Intent Router、Session/Agent 真值。`apps/web` 不再拥有产品业务组件。
