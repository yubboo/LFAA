/** Shell 全局快捷键。只调用公开 Controller callback，不持有业务状态。 */
import { useEffect } from "react";
export function useWorkbenchShellShortcuts({onEscape,onOpenSettings,onToggleLeft,onToggleRight,onToggleTerminal}:{onEscape:()=>void;onOpenSettings:()=>void;onToggleLeft:()=>void;onToggleRight:()=>void;onToggleTerminal:()=>void}){
  useEffect(()=>{const onKeyDown=(event:KeyboardEvent)=>{if(event.key==="Escape"){onEscape();return;}if(event.ctrlKey&&!event.altKey&&!event.shiftKey&&event.key===","){event.preventDefault();onEscape();onOpenSettings();return;}const target=event.target as HTMLElement|null;const tag=target?.tagName;if(tag==="INPUT"||tag==="TEXTAREA"||target?.isContentEditable)return;if(event.ctrlKey&&!event.altKey&&event.key.toLowerCase()==="b"){event.preventDefault();onToggleLeft();}else if(event.ctrlKey&&event.altKey&&event.key.toLowerCase()==="b"){event.preventDefault();onToggleRight();}else if(event.ctrlKey&&!event.altKey&&!event.shiftKey&&event.key.toLowerCase()==="j"){event.preventDefault();onToggleTerminal();}};window.addEventListener("keydown",onKeyDown);return()=>window.removeEventListener("keydown",onKeyDown);},[onEscape,onOpenSettings,onToggleLeft,onToggleRight,onToggleTerminal]);
}
