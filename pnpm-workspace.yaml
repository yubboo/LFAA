packages:
  - "apps/*"
  - "packages/*"

# Native dependencies with install/build scripts must be reviewed and pinned.
# node-pty is required by the local real terminal bridge.
strictDepBuilds: true
allowBuilds:
  "node-pty@1.1.0": true
