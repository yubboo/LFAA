# LFAA 项目结构与代码地图

> 这份文档专门解决“这个文件夹是什么、这个文件是干什么的、UI 从哪里开始看”的问题。
> 第一次打开项目时，建议先看本文件，再进入具体源码。

## 1. 最先记住的四个入口

如果只想知道项目怎么读，先记这四个：

```text
README.md
→ 项目是什么、怎么启动

DEVELOPMENT.md
→ 开发时必须遵守什么规则

docs/项目结构与代码地图.md
→ 每个主要目录 / 文件是干什么的

ARCHITECTURE.md
→ 各模块在整个系统中的上下游关系
```

如果是修改 Web 工作台 UI，再按下面顺序：

```text
apps/web/src/App.tsx
→ packages/app-shell/src/AgentWorkbench.tsx
→ packages/app-shell/src/agent-workbench.css
→ packages/ui/src/workbench/ResizableWorkbench.tsx
→ packages/ui/src/workbench/workbench.css
→ packages/ui/src/features/settings/SettingsPage.tsx（与主工作台共享 leftPaneWidth）
→ apps/web/src/LocalTerminal.tsx
→ apps/web/src/local-terminal.css
```

---

## 2. 根目录：你在资源管理器第一眼看到的东西

### 2.1 根目录文件夹

| 路径 | 人话解释 | 什么时候需要看 |
|---|---|---|
| `.git/` | Git 历史数据库。保存 Commit、分支、远程信息。只存在稳定工作区，不属于发布包内容。 | Git / Push 出问题时；平时不要手改。 |
| `.lfaa/` | 当前项目自己的 Skills、Experts、Plugins、Extensions、MCP 资源根。 | 做 Agent 扩展、插件、技能时。 |
| `apps/` | 真正可以运行起来的应用入口。 | 看 Web、Desktop、CLI、Server 从哪里启动。 |
| `crates/` | Rust 原生能力和安全 Broker。 | 做文件、进程、PTY、Sandbox、Secret 等系统能力时。 |
| `docs/` | 所有开发规范、架构、计划、日志、版本记录。 | 不知道“为什么这样做”时先看这里。 |
| `evals/` | Agent Eval 的工程级测试资产入口。 | 后续做 Agent 回归测试时。 |
| `node_modules/` | pnpm 安装出来的本地依赖缓存。不是手写源码。 | 一般不要看、不要改、不要提交为版本包差异。 |

> v0.0.55 起，`LFAA-Setup.bat → 1/7` 会直接打印依赖位置：项目 `node_modules`、`node_modules/.pnpm`、`pnpm store path`、`pnpm-lock.yaml`、Cargo registry/git cache、Rust toolchains 与 Cargo.lock，方便排错时快速定位。
> v0.0.56 起，菜单 1 还会把“项目依赖真实可解析”和“pnpm Store 缓存健康”分开判断；Store 路径存在并不代表缓存完整，状态缓存也不代表真实依赖可用。
> v0.0.57 起，菜单 1 / 7 还会实时显示 pnpm executable、PNPM_HOME、active Store 与 Store 来源；仓库不设置项目级 `storeDir`，历史 `.lfaa/state` 不得回放旧机器路径。
| `packages/` | TypeScript/React 的共享业务模块和 UI 模块。 | 绝大部分应用逻辑会逐步放这里。 |
| `scripts/` | 项目治理、检查、Windows 菜单背后的实现脚本。 | Setup / Sync / GitHub / Update 或治理检查出问题时。 |

### 2.2 根目录主要文件

| 文件 | 作用 |
|---|---|
| `README.md` | 项目总入口：产品是什么、怎么启动、当前阶段。 |
| `DEVELOPMENT.md` | 当前唯一有效开发流程；规定开发前读什么、开发后更新什么。 |
| `AGENTS.md` | 给 AI / Agent 的开发入口，重复强调阅读顺序和边界。 |
| `ARCHITECTURE.md` | 当前系统总架构和不可破坏的依赖方向。 |
| `PROJECT_PLAN.md` | 当前开发顺序和模块状态。 |
| `CHANGELOG.md` | 最近版本变更索引。 |
| `lfaa.release.json` | 当前发布版本号和架构/协议版本号的机器可读事实源。 |
| `package.json` | Node workspace 根配置、pnpm 脚本、核心前端依赖。 |
| `pnpm-workspace.yaml` | pnpm workspace 范围和原生依赖构建白名单。 |
| `pnpm-lock.yaml` | Node 依赖精确锁定结果。不要手工随意编辑。 |
| `Cargo.toml` | Rust workspace 根配置。 |
| `rust-toolchain.toml` | 本项目要求的 Rust 工具链版本。 |
| `tsconfig.base.json` | TypeScript workspace 共用基础配置。 |
| `turbo.json` | monorepo 任务编排配置。 |
| `.editorconfig` | 编辑器通用格式约定。 |
| `.env.example` | 环境变量示例，不保存真实 Secret。 |
| `.gitignore` | Git 忽略规则。 |
| `NOTICE.md` | 项目归属和第三方声明入口。 |
| `LFAA-Setup.bat` | Windows 开发环境总菜单入口。 |
| `LFAA-Sync.bat` | 版本包 → 稳定工作区同步入口。 |
| `LFAA-GitHub.bat` | Commit / Push 菜单入口。 |
| `LFAA-Update.bat` | 已 Clone Git 仓库的源码更新入口。 |

---

## 3. `apps/`：可以启动的应用

`apps/` 只放“应用宿主入口”；可复用业务和 Feature UI 必须下沉到 `packages/`。Web-first 只是当前验证顺序，不改变所有权。

| 路径 | 作用 |
|---|---|
| `apps/web/` | 当前 Web 验证宿主：Vite + React 启动、Router、Web Host Adapter、本地开发桥；不拥有共享业务 UI。 |
| `apps/desktop/` | Electron 桌面应用骨架。 |
| `apps/server/` | 后续 Agent Server / Remote Runtime 入口。 |
| `apps/cli/` | 命令行入口。 |

每个 app 里常见文件：

```text
package.json   → 这个 app 的包名、依赖、脚本
README.md      → 这个 app 的职责说明
tsconfig.json  → 这个 app 的 TypeScript 配置
src/           → 真正源码
```

### 3.1 当前 Web 入口怎么串起来

```text
apps/web/index.html
→ apps/web/src/main.tsx
→ apps/web/src/App.tsx
→ @lfaa/app-shell / AgentWorkbench
```

`apps/web/vite.config.ts` 不负责页面长什么样，它负责本地 Vite 服务以及两个开发桥：

```text
.lfaa 资源元数据桥
真实终端 node-pty 桥
```

---

## 4. `packages/`：共享 TypeScript / React 模块

### 4.1 当前和 UI 最相关的两个包

| 路径 | 作用 |
|---|---|
| `packages/app-shell/` | 页面壳与区域编排。决定左栏、中间区、右栏、顶栏、输入框里放什么。 |
| `packages/ui/` | 可复用图形界面唯一主目录：Primitive / Layout / Workbench / Feature UI；不拥有业务事实状态。 |

### 4.2 其余 package 职责

| Package | 作用 |
|---|---|
| `account-manager` | API / OAuth / Local / 官方允许的账号认证配置。 |
| `agent-client` | Electron / HTTP / SSE / WS 的统一客户端传输层。 |
| `agent-loop` | Agent Observe → Decide → Act → Verify 生命周期。 |
| `agent-runtime` | RunManager 与 Agent 核心组合入口。 |
| `builtin-tools` | 内置 fs/search/git/shell 等 Tool Adapter。 |
| `code-intelligence` | ripgrep、Tree-sitter、Git、未来 LSP。 |
| `config-system` | 配置设置业务唯一归属：Schema、Settings、Account/Auth、AI Provider 配置插件、后续 Storage；不包含 React UI / 模型推理 Runtime。 |
| `context-engine` | Working Set、上下文预算、投影与压缩。 |
| `document-parser` | PDF / DOCX / Markdown / Text 解析管线。 |
| `domain` | Session、Run、Tool、Permission、Agent 等稳定领域类型。 |
| `evals` | Agent Fixture、Runner、Baseline、Regression。 |
| `event-store` | Durable Session / Run 的 append-only 事实记录。 |
| `job-engine` | 长任务、暂停、恢复、取消、Crash Recovery。 |
| `knowledge-core` | 知识源、文档、Chunk、索引抽象。 |
| `mcp-host` | MCP Client/Server 生命周期、Adapter、权限映射。 |
| `model-core` | 模型 Provider 的统一能力接口。 |
| `model-providers` | 各厂商 / 本地模型 Adapter。 |
| `model-router` | 按能力、成本、健康状态选择模型。 |
| `observability` | Trace、Metrics、Cost、Debug Timeline。 |
| `permission-engine` | Ask / Auto / Full 与审批生命周期。 |
| `plugin-runtime` | Native Plugin 生命周期和能力注册。 |
| `plugin-sdk` | 第三方插件公共 SDK 与权限声明。 |
| `policy-engine` | 确定性 Allow / Ask / Deny 决策。 |
| `protocol` | UI / Runtime / Desktop / Web / Child Agent 版本化消息。 |
| `retrieval` | FTS、Vector、Hybrid、Rerank。 |
| `skill-runtime` | `SKILL.md` 发现、按需加载、执行上下文。 |
| `subagent-runtime` | Parent / Child Run、隔离 Context、预算、结果协议。 |
| `tool-runtime` | Tool 执行唯一业务入口。 |
| `tool-sdk` | Tool 开发公共接口。 |
| `usage-manager` | Token、Cost、Quota、Rate Limit。 |
| `verifier` | 确定性验证和可选 Reviewer 验证。 |
| `workspace` | Workspace、Grant、路径范围和项目元信息。 |

这些包目前有不少还是**边界骨架**，不是都已经实现完整业务。每个包自己的 `README.md` 会说明当前状态。

### 4.3 配置设置与 UI 的固定父子级

```text
packages/config-system/src/settings/ai/
├── core/
└── providers/<provider>/

packages/ui/src/features/settings/ai/

当前 v0.0.74 关键源码导航：

```text
packages/config-system/src/settings/ai/core/provider.types.ts
→ Provider 插件公共契约

packages/config-system/src/settings/ai/core/provider-registry.ts
→ 无厂商分支的注册表
packages/config-system/src/settings/ai/core/account-service.ts
→ Provider 无关 Account/Auth/Model 业务闭环；保存前校验官方模型目录与 Model Capability。

packages/config-system/src/settings/ai/core/model-settings.ts
→ 官方 Model Capability 配置值白名单校验与默认值。


packages/config-system/src/settings/ai/providers/<provider>/plugin.ts
→ 每一家厂商的配置事实

packages/ui/src/features/settings/SettingsPage.tsx
→ 独立设置中心

packages/ui/src/features/settings/ai/AiSettingsPanel.tsx
→ Settings 内可复用 AI Provider 配置内容

apps/web/dev/bridges/ai/ai-config-bridge.ts
→ Web 开发宿主 localhost AI Bridge；调用 Config System，不拥有 Provider 业务。

apps/web/dev/bridges/ai/rust-secret-store.ts
→ Web Host 的 Rust Secret Broker Adapter；Secret 只经二进制 stdin/stdout 协议。

crates/secret-store/src/lib.rs
→ Rust Secret Broker；Windows 直接 FFI 调用 CredWriteW / CredReadW / CredDeleteW。

apps/web/src/host/ai-settings-client.ts
→ 浏览器同源 Host Client；不保存 Secret。

packages/app-shell/src/AgentWorkbench.tsx
→ Registry → UI ViewModel 组装、Settings/UserMenu/Theme 交互状态
```
```

查 AI 配置业务只进 `config-system/src/settings/ai`；查 AI 设置界面只进 `ui/src/features/settings/ai`。`apps/web` 只负责把两者通过宿主 Adapter 组装起来，不再保存第二套业务页面或 Provider 逻辑。

同一家厂商的“配置插件”和“推理 Runtime Adapter”职责不同：前者属于 Config System，后者属于模型运行域。目录分层按职责，不按厂商名字强行合并。

---

## 5. `crates/`：Rust 原生系统能力

TypeScript/React 不能直接越过权限层访问 OS。真正系统能力最终通过 Rust Broker。

| Crate | 作用 |
|---|---|
| `native-core` | Rust 原生能力总入口与 Broker 组合。 |
| `native-protocol` | TypeScript / Rust 之间的原生协议类型。 |
| `fs-broker` | 文件系统访问。 |
| `process-broker` | 系统进程执行。 |
| `pty-broker` | Terminal / PTY。 |
| `sandbox` | Sandbox / 进程隔离边界。 |
| `secret-store` | OS Credential Store / Secret Broker；v0.0.73 起 Windows Generic Credential 正式由 Rust FFI 实现。 |
| `workspace-security` | canonical path、项目范围、安全校验。 |
| `file-watcher` | 项目资源目录监听。 |

当前大部分 crate 仍处于骨架阶段，具体状态看各自 `README.md`。

---

## 6. `scripts/`：不是业务源码，而是开发工具

| 文件 | 作用 |
|---|---|
| `scripts/governance-check.mjs` | 检查项目治理骨架和关键约束。 |
| `scripts/import-path-check.mjs` | 检查 TypeScript 导入边界。 |
| `scripts/dev-log-check.mjs` | 检查开发日志命名、索引和历史连续性。 |
| `scripts/docs-check.mjs` | 检查 docs 目录结构和文档命名。 |
| `scripts/comment-check.mjs` | 检查关键实现文件是否有结构化中文文件头和 CSS 分区注释。 |
| `scripts/config-schema-check.mjs` | 检查 Config Schema 单一版本源、credentialRef 与 Secret 明文字段安全边界。 |
| `scripts/check-node-pty.mjs` | 验证 `node-pty` 是否真的可加载。 |
| `scripts/node-dependency-health-check.mjs` | 从每个 workspace 的真实 importer 解析外部依赖；不会读取依赖状态缓存来冒充健康。 |
| `scripts/pnpm-only.mjs` | 强制 pnpm，并要求实际版本与根 packageManager / engines.pnpm 精确一致。 |
| `scripts/quality-not-configured.mjs` | 历史占位失败入口；当前根级 typecheck / test / build 已改为真实聚合命令，不再使用它。 |
| `scripts/release-name.mjs` | 读取版本信息生成发布名。 |
| `scripts/release-environment-check.mjs` | 检查 Node 24.x、pnpm 11.17.0 与 lockfile 发布环境。 |
| `scripts/release-rust-check.mjs` | 执行 Rust workspace `cargo check/test`。 |
| `scripts/release-gates-check.mjs` | 静态防回归检查 quick / full / release 三层质量命令、Setup 检查中心与增量依赖契约。 |
| `scripts/windows-script-encoding-check.mjs` | 检查 Windows PowerShell `.ps1` 的 UTF-8 BOM 与 BAT 调用关系。 |
| `scripts/release-consistency-check.mjs` | 检查当前产品版本、package/crate、README、CHANGELOG、Release 是否一致。 |
| `scripts/windows/lfaa-setup.ps1` | `LFAA-Setup.bat` 背后的 Windows 环境准备、依赖指纹复用与检查中心 Adapter；菜单编号不是 CLI API。 |
| `scripts/windows/lfaa-sync.ps1` | `LFAA-Sync.bat` 背后的实际逻辑。 |
| `scripts/windows/lfaa-github.ps1` | `LFAA-GitHub.bat` 背后的实际逻辑。 |
| `scripts/windows/lfaa-update.ps1` | `LFAA-Update.bat` 背后的实际逻辑。 |
| `test/dependency-setup.test.mjs` | 锁定菜单 1 增量依赖、实时 pnpm 环境事实，以及 Windows PowerShell 自动/只读变量不得被普通赋值的防回归契约。 |

---

## 7. `docs/`：不是“堆文档”，每一类有固定职责

| 目录 | 负责回答什么问题 |
|---|---|
| `standards/` | **以后怎么写？** 当前有效开发规范。 |
| `architecture/` | **系统怎么分层？为什么这样分？** 当前和历史架构。 |
| `modules/` | **某个业务模块负责什么？** 模块职责。 |
| `plans/` | **准备怎么做？** 开发计划。 |
| `progress/` | **现在做到哪？** 开发进度。 |
| `prompts/` | **这次任务原始合同是什么？** Active / Archive Prompt。 |
| `logs/development/` | **需求/设计后来怎么变了？** 开发决策日志。 |
| `logs/runtime/` | **脚本刚刚运行发生了什么？** Sync/GitHub/Update 机器运行日志。 |
| `changelog/` | **每个版本改了什么？** 用户版本变更。 |
| `releases/` | **这个发布包交付了什么？** 发布记录。 |
| `testing/` | **怎么验证？** 测试策略。 |

### 7.1 为什么以前你会觉得 docs 很多但看不懂代码

之前的 docs 主要做的是：

- 记录规范；
- 记录历史需求；
- 记录架构边界；
- 记录版本变更；
- 给治理脚本提供事实源。

它们**没有做好“人类第一次打开项目时的代码导航”**，也没有把关键源码文件的文件头注释强制落地。这就是本文件新增的原因。

---

## 8. 当前 Web UI 到底由哪些文件负责

这是当前最重要的代码关系。

### 8.1 宿主层

```text
apps/web/src/App.tsx
```

负责：

- 从 Vite 拉 `.lfaa` 资源元数据；
- 监听资源变更；
- 把资源和 `LocalTerminal` 传给 `AgentWorkbench`。

它**不负责 UI 布局算法**。

### 8.2 App Shell 层

```text
packages/app-shell/src/AgentWorkbench.tsx
packages/app-shell/src/agent-workbench.css
```

`AgentWorkbench.tsx` 决定“有哪些区域、里面有什么、按钮控制什么”。

当前结构（v0.0.47）：

```text
AgentWorkbench
└─ Workbench Stage（ResizeObserver 观察真实容器尺寸）
   ├─ Left Hover Preview（Desktop / Compact，左栏收起后临时出现）
   └─ ResizableWorkbench
      ├─ LeftSidebar
      ├─ CenterWorkspace
      │  ├─ var(--agent-shell-header-h) Center Header
      │  │  ├─ 左栏按钮 + Web 工作台标题
      │  │  └─ 更多 / 分享 / 响应式 Shell Actions
      │  ├─ Conversation
      │  └─ Composer
      ├─ RightSidebar
      │  ├─ Desktop：Right Shell Header
      │  └─ 工具与资源正文
      └─ BottomTerminal
```

响应式现在不是固定断点，而是：

```text
Workbench Stage 宽高
→ workbench-layout.config.ts
→ ratio / floor / ceiling
→ 计算左右栏 + 中央区能否同时容纳
→ Desktop / Compact / Mobile
```

参考结果：1024 左右仍可双 Dock；950 / 820 / 760 左右通常为左 Dock + 右 Overlay；约 640 及更窄进入双 Overlay。边界由公式决定，不是硬编码 viewport breakpoint。

`agent-workbench.css` 负责内容盒子视觉，并使用 `--agent-*` 变量、`rem`、`clamp()`、`calc()`；**不负责三栏拖拽数学**。

### 8.3 纯 Layout 层

```text
packages/ui/src/workbench/workbench-layout.config.ts
packages/ui/src/workbench/ResizableWorkbench.tsx
packages/ui/src/workbench/workbench.css
```

负责：

- 单一布局 Token：ratio / floor / ceiling / center / separator / hysteresis；
- 依据容器宽高计算 Desktop / Compact / Mobile；
- 左右栏宽度与历史持久化宽度重新 clamp；
- separator 拖拽；
- 动态 max 与中央区保护；
- 到动态 min 吸附收起 / Pointer Capture；
- 底部终端高度；
- 按住 Pointer 时从吸附状态反向拖回 min；
- Pointer Up 后正式收起，separator 禁止反向展开；
- Desktop 双 Dock / Compact 右 Overlay / Mobile 双 Overlay 几何。

当前动态 min 大致：左 196~232、右 228~288、Bottom 136~176；这些不是业务固定值。

不关心左栏里到底是“项目列表”还是别的内容。

### 8.4 真实终端层

```text
apps/web/src/LocalTerminal.tsx
apps/web/src/local-terminal.css
apps/web/vite.config.ts
```

数据流：

```text
用户键盘
→ xterm.js
→ Vite HMR 事件
→ node-pty
→ PowerShell / Shell

Shell 输出
→ node-pty
→ Vite HMR 事件
→ xterm.js
→ 浏览器显示
```

`vite.config.ts` 只在本地开发模式提供这个桥，不是正式 Agent Tool Runtime。

---

## 9. CSS 怎么看：哪个文件管哪个盒子

不要把三个 CSS 文件混着看：

```text
agent-workbench.css
→ “里面长什么样”
→ 顶栏、左栏内容、中间内容、按钮、右栏、输入框、终端外壳

workbench.css
→ “这些大盒子怎么摆、怎么拉”
→ Grid、左右宽度、separator、min 吸附收起、底部高度、Dock / Drawer 响应式

local-terminal.css
→ “终端黑盒内部怎么铺满”
→ xterm host、viewport、状态角标
```

三个 CSS 文件从 v0.0.41 起都有：

- 文件头职责；
- 关联 TS/TSX 文件；
- 盒子结构；
- 分区编号注释。

---

## 10. 常见文件扩展名是什么

| 后缀 | 人话解释 |
|---|---|
| `.tsx` | React 组件，既有 TypeScript 又有 JSX UI 结构。 |
| `.ts` | TypeScript 逻辑或类型。 |
| `.css` | 样式。 |
| `.mjs` | Node.js ESM 脚本，主要用于治理/检查。 |
| `.ps1` | PowerShell 脚本，Windows 菜单背后的真正逻辑。 |
| `.bat` | Windows 双击入口，只负责调用 `.ps1`。 |
| `.rs` | Rust 源码。 |
| `.toml` | Rust / 工具配置。 |
| `.json` | 机器可读配置。 |
| `.yaml` | pnpm workspace 等配置。 |
| `.md` | 人类阅读文档。 |

---

## 11. 新代码的可读性要求

以后关键实现文件不能只“代码能跑”就算完成。

必须同时做到：

1. 文件开头说明“作用 / 负责 / 不负责 / 状态归属 / 对外接口 / 关联文件 / 修改注意事项”；
2. 复杂算法写“为什么这样做”，不是逐行翻译；
3. CSS 按页面区域 / 盒子分区；
4. 新增顶层业务目录必须有 README；
5. 目录职责变化必须同步本项目地图；
6. `scripts/comment-check.mjs` 作为治理门禁防止关键文件再次漏注释。

### v0.0.48 左栏宽度链路

`ResizableWorkbench.leftWidth → onLeftWidthChange → AgentWorkbench.leftPaneWidth → --agent-left-preview-width → Hover Preview`。Hover 不是第二套侧栏，只是正式左栏真实宽度的临时投影。


## 文档治理脚本（v0.0.50）

```text
scripts/docs-check.mjs              → 固定 9 个长期 docs Markdown
scripts/dev-log-check.mjs           → 单文件开发日志编号与当前任务
scripts/prompt-lifecycle-check.mjs  → Prompt / AI 自测 / 用户验收状态
scripts/release-consistency-check.mjs → 当前版本在 package / crate / Changelog / Releases 一致
scripts/release-gates-check.mjs       → 发布环境 / frozen install / 真实质量命令防回归
```

- v0.0.59：`scripts/windows/lfaa-setup.ps1` 分离开发期 lockfile 同步与 frozen 本地修复，并稳定透传 pnpm 写操作进度。
- v0.0.60：菜单 1 撤销强制 append-only reporter，交互式 pnpm install 恢复 pnpm 原生前台输出；LFAA 不捕获或模拟安装进度。


## 12. v0.0.63 AI 配置目录导航

业务与 UI 从本版本开始严格分家：

```text
packages/config-system/src/settings/ai/
├─ core/                     Provider 公共契约与 Registry
├─ transports/               多 Provider 确认共享的配置期协议
└─ providers/
   ├─ openai/
   ├─ deepseek/
   ├─ zhipu/
   ├─ kimi/
   ├─ qwen/
   └─ xiaomi/

packages/ui/src/features/settings/ai/
├─ AiSettingsPage.tsx        可复用图形设置页
├─ ai-settings.types.ts      纯 ViewModel/Props
└─ ai-settings.css           Feature 自有样式

packages/app-shell/src/AgentWorkbench.tsx
→ 只负责把 Config System Provider Registry 映射为 UI ViewModel 并编排页面。

apps/web/
→ 只作为 Web 宿主，不保存第二套 Provider / Account / UI 业务。
```

自动边界：`scripts/folder-boundary-check.mjs` 与 `scripts/import-path-check.mjs`。

> 当前候选版本：v0.0.64（#2.4，pending-user-acceptance）。
### v0.0.64 Settings / 个人中心 UI

```text
packages/ui/src/features/account/UserMenu.tsx
→ 共享个人中心菜单；不拥有登录/套餐真值。

packages/ui/src/features/appearance/ThemeModeMenu.tsx
→ system / light / dark 三态主题选择 UI。

packages/ui/src/features/settings/SettingsPage.tsx
→ 独立设置中心：左导航 + 右内容。

packages/ui/src/features/settings/ai/AiSettingsPanel.tsx
→ Settings 内的 AI Provider 配置内容。

packages/app-shell/src/AgentWorkbench.tsx
→ 打开/关闭菜单、Settings Surface、主题 preference 与系统主题监听。
```



### Settings 左栏共享几何（v0.0.69）

- `packages/ui/src/features/settings/SettingsPage.tsx`：设置中心内容/导航状态；直接使用 ResizableWorkbench；
- `packages/ui/src/workbench/ResizableWorkbench.tsx`：工作台和设置页共同的左栏 resize / snap / release / 持久化唯一实现；
- `packages/ui/src/workbench/workbench-interaction.config.ts`：Workbench capture ratio / hysteresis / 动画时长 / 键盘 resize 步长的统一可调参数。
- `packages/ui/src/features/settings/settings.css`：仅内容视觉，不允许固定侧栏几何。


### UI Workspace 运行时公共导入（v0.0.70）

- `packages/ui/src/workbench/index.ts`：`@lfaa/ui/workbench` 稳定公共子入口；
- `packages/ui/package.json#exports["./workbench"]`：宿主无关的真实运行时解析事实；
- `packages/ui/src/features/settings/SettingsPage.tsx`：通过公共子入口复用 Workbench，不再使用 `@/workbench/*`；
- `scripts/runtime-import-resolution-check.mjs`：验证 workspace 公共 subpath Export 与目标文件，阻止 TypeScript-only alias 假通过。
