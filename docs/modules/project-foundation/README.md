# project-foundation

## 作用

建立 LFAA 的仓库骨架、治理、规范、架构入口、文档体系与模块边界。

## 状态

```text
deliverable
```

## 不负责

不实现真实 Agent 业务、不实现配置 UI、不连接真实模型。

## v0.0.2 优化

新增 `@/` / `@lfaa/*` 导入路径规范和自动检查，防止深层相对路径破坏模块边界。
