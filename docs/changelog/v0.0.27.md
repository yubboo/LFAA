# LFAA v0.0.27

## #21.3 最小宽度自动吸附

### 问题

v0.0.26 的侧栏收起逻辑是：

```text
拖到独立 96px 阈值
→ 松开 Pointer
→ 才收起
```

这与目标交互不一致。

### 修改

现在改为：

```text
左栏到 240px
→ 立即自动吸附收起

右栏到 300px
→ 立即自动吸附收起
```

不需要松开鼠标。

反向拖回时使用 24px 迟滞，避免最小宽度边界反复开合。

最大宽度、中央最小宽度保护和 localStorage 状态继续保留。
