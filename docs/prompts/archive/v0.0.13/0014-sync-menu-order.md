# #14 同步菜单顺序优化

## 主模块

`project-foundation`

## 目标

调整 `LFAA-Sync.bat` 菜单顺序，让最高频操作放在数字 `1`。

## 新顺序

```text
1 执行同步
2 预览差异
3 同步配置
0 退出
```

## 原因

日常最常用操作是实际同步，因此数字 `1` 应直接进入执行同步流程。

## 不修改

- 同步安全机制；
- SHA-256 校验；
- Git 保护；
- config-system 业务。
